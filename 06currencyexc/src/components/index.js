// We made index.js so that when we want to export many component instead of importing each one of them individually we can directly import this file which will contain all the components

import InputBox from "./InputBox";

export {InputBox}